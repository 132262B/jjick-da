<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jjickda.domain.admin.repository.AdminRepository">

    <!-- NULL 은 굳이 넣을 필요 없음 -->
    <insert id="registMain">
        INSERT
          INTO exam_main_category
             ( main_ctg_name
             , use_status
             , reg_date
             , reg_seq
             )
        VALUES
             ( #{question.main_ctg_name}
             , 1
             , now()
             , 0
             );
    </insert>

</mapper>